<UserControl x:Class="Selenite.Client.Views.WebAutomation.EditCategoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0"
                       Text="Categories"/>

            <ListBox Grid.Row="1" Grid.Column="0"
                     ItemsSource="{Binding Categories}"
                     SelectedItem="{Binding SelectedCategory}"
                     DisplayMemberPath="Name"/>

            <Grid Grid.Row="2" Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <CheckBox Grid.Row="0"
                          IsChecked="{Binding IsEnabled}"
                          Content="Enabled"/>

                <TextBlock Grid.Row="1"
                           Text="Name" />
                <TextBox Grid.Row="2"
                         Text="{Binding Name}"/>

                <TextBlock Grid.Row="3"
                           Text="Domain" />
                <TextBox Grid.Row="4"
                         Text="{Binding Domain}"/>

                <Button Grid.Row="5"
                        Margin="5"
                        Content="Add"/>

                <Button Grid.Row="6"
                        Margin="5"
                        Content="Remove"/>
            </Grid>

            <TextBlock Grid.Row="0" Grid.Column="1"
                       Text="Tests"/>

            <ListBox Grid.Row="1" Grid.Column="1"
                     ItemsSource="{Binding SelectedCategory.PageTests}"
                     SelectedItem="{Binding SelectedTest}"
                     DisplayMemberPath="Name"/>

            <TextBlock Grid.Row="0" Grid.Column="2"
                       Text="Commands"/>

            <ListBox Grid.Row="1" Grid.Column="2"
                     ItemsSource="{Binding SelectedTest.Commands}"
                     SelectedItem="{Binding SelectedCommand}"
                     DisplayMemberPath="Name"/>

            <TextBlock Grid.Row="0" Grid.Column="3"
                       Text="Properties"/>

            <ListBox Grid.Row="1" Grid.Column="3"
                     ItemsSource="{Binding SelectedCommand.Properties}"
                     SelectedItem="{Binding SelectedCommand}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <WrapPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Key}"/>
                            <TextBlock Text="{Binding Value}"/>
                        </WrapPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
        
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <Button Grid.Column="0"
                    Content="Save Changes"
                    Width="100"
                    Command="{Binding SaveCommand}"/>
            
            <Button Grid.Column="2"
                    Content="Cancel"
                    Width="100"
                    Command="{Binding CancelCommand}"/>
        </Grid>
    </Grid>
</UserControl>